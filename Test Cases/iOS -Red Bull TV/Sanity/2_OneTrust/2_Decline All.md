Precondition
```
# A new instance of `<application>` is installed
1. Launch `<application>` from a fresh install
2. Observe the Consent Management Prompt (CMP)
```

Step 1:
```
### Verify "Decline All" button sets all SDK toggles to "ON"
1. Select "Decline All"
2. Navigate to "Account > Your Privacy"
```
```
All SDK toggles should be set to the "OFF" position.
```