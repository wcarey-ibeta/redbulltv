Precondition
```
# No instance of `<application>` is installed
```
Step 1:
```
Perform a fresh install of `<application>` on `<platform>`
```
```
The application will install without error.
> The application icon may differ between Release build (production-only) and Switcher build (QA environment available).

![](https://wcarey-ibeta.github.io/redbulltv/images/ios/install/appicons.png)
```